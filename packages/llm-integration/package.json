{
  "name": "@active-collaboration/llm-integration",
  "version": "0.1.0",
  "private": true,
  "description": "LLM provider abstraction supporting Ollama and SiliconFlow with multi-model strategy",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest",
    "test:run": "tsx src/test.js",
    "test:multi-model": "tsx src/test-multi-model.ts",
    "test:coverage": "vitest --coverage"
  },
  "dependencies": {
    "@active-collaboration/shared": "*",
    "cross-fetch": "^4.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^1.6.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.1"
  }
}
