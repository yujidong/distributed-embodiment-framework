{
  "name": "active-collaboration-framework",
  "version": "1.0.0",
  "private": true,
  "description": "Active Collaboration Framework - Distributed Embodiment for IoT with LLM Agents",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build packages/shared packages/llm-integration packages/core && tsc --build packages/simulation",
    "build:shared": "npm run build -w @active-collaboration/shared",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "turbo": "^1.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
